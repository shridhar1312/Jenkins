pipeline {
    agent {
        docker {
            image 'alpine:3.14'
            args '--user root -v /var/run/docker.sock:/var/run/docker.sock'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'sleep 10'
            }
        }
    }
}
